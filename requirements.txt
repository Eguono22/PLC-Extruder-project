# Core simulation platform works with the Python standard library.
# The application layer adds FastAPI for the operator API and dashboard.
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0
asyncua>=1.1,<2.0
pymodbus>=3.6,<4.0
