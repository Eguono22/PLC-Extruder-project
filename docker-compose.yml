services:
  extruder-app:
    build: .
    container_name: extruder-app
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./runtime_logs:/app/runtime_logs
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/health/live', timeout=3)"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
